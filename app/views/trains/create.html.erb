<p>
  <h3>You're departing from:</h3>
  <%= @start_station.name %>
</p>

<hr>

<h3>Select a train:</h3>
<% @start_station.departures.each do |d| %>
	<strong><p class="text-uppercase">
		<%= d.destination.name %>
	</p></strong>
	<p>
		<% d.estimates.each do |e| %>
			<%= link_to "#{e.minutes} min (#{(Time.now + e.minutes*60).strftime("%-l:%M%P")})", reminder_path(min: e.minutes, phone: @phone), class: "btn btn-default" %>
		<% end %>
	</p>
<% end %>